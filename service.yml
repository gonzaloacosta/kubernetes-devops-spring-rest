apiVersion: v1
kind: Service
metadata:
  creationTimestamp: null
  labels:
    app: devops-spring-rest
  name: devops-spring-rest
spec:
  ports:
  - port: 8080
    protocol: TCP
    targetPort: 8080
  selector:
    app: devops-spring-rest
status:
  loadBalancer: {}
